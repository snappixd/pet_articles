CRUD-приложение:

Запросы(endpoints):
1) GET для всіх статтів("/")
2) GET для однієї статті("/getArticle/:id")
3) GET для видалення статті("/deleteArticle/:id")
4) POST для створення статті("/createArticle")

БД:
1) MongoDB(db: ***; collection: ***)

GIT: 
1) Запушити на github репозиторій
2) Запушити на heroku
